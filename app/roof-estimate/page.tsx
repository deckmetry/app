import { RoofEstimator } from "@/components/roof/roof-estimator";

// Internal roof / outdoor-living estimator. Unlinked from public nav.
export const metadata = { title: "Roof Estimator (Internal) — Deckmetry" };

export default function RoofEstimatePage() {
  return <RoofEstimator />;
}
